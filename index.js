// const portStart = parseInt(process.env && process.env.portStart || 3101)
// global.env = process.env.NODE_ENV || 'prod'
// global.PMID = parseInt(process.env && process.env.pm_id || 0)
// global.instanceId = parseInt(process.env && process.env.NODE_APP_INSTANCE || 0)
// global.serverPort = portStart + instanceId

// global.config = require('./config/')
// global.redis = require('./modules/redis/')
// global.log = require('./api/log')
// global.api = require('./api/index')
// global.util = require('./src/util/')

